@page
@model ScoreManagement.Pages.AdminMenu.StudentClassesManage.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentClass[0].Class)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentClass[0].Student)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.StudentClass) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Class.ClassCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.StudentCode)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StudentClassId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StudentClassId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.StudentClassId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
